[project]
name = "shared-utils"
version = "0.1.0"
description = "SiliconPulse shared utilities (IDs, time, pagination helpers)"
requires-python = ">=3.12"
dependencies = [
  "orjson>=3.10,<4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


